name: Keep HF Spaces Alive

on:
  schedule:
    # Cron pakai UTC. '0 3 * * *' = 03:00 UTC = 10:00 WIB tiap hari.
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  ping:
    name: Ping ${{ matrix.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: capek-mikir
            url: https://capek-mikir-7ik.pages.dev
          - name: identity
            url: https://andreasmlbngaol-identity.hf.space
          - name: tomavue-fe
            url: https://tomavue.pages.dev
          - name: tomavue-be
            url: https://andreasmlbngaol-tomavue-be.hf.space/api/health
          - name: tomavue-ai
            url: https://andreasmlbngaol-tomavue-ai.hf.space/health
    steps:
      - name: Ping Space
        env:
          NAME: ${{ matrix.name }}
          URL: ${{ matrix.url }}
        run: |
          echo "Pinging $NAME -> $URL"
          code=$(curl -sS -L \
            --retry 5 --retry-delay 15 --retry-all-errors \
            --connect-timeout 30 --max-time 120 \
            -o /dev/null -w "%{http_code}" \
            "$URL")
          echo "HTTP status: $code"
          if [ "$code" -ge 200 ] && [ "$code" -lt 400 ]; then
            echo "✅ $NAME is awake (HTTP $code)"
          else
            echo "❌ $NAME returned HTTP $code"
            exit 1
          fi
